package br.edu.ifpr.appaurico.ui.theme

import androidx.compose.ui.unit.dp

/** Tokens de espacamento e ritmo, aplicados de forma consistente em todas as telas. */
object AuricoDimens {
    /** Padding horizontal nas bordas da tela. */
    val ScreenPadding = 20.dp

    /** Espacamento vertical entre cards/blocos. */
    val BlockSpacing = 16.dp

    /** Respiro interno dos cards. */
    val CardPadding = 16.dp

    /** Raio de arredondamento de cards e botoes. */
    val CornerRadius = 16.dp
}
